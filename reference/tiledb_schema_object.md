# Succinctly describe a TileDB array schema

This is an internal function that is not exported.

## Usage

``` r
tiledb_schema_object(array)
```

## Arguments

- array:

  A TileDB Array object

## Value

A list containing two data frames, one describing the overall array as
well as one with descriptions about dimensions and attributes in the
schema
