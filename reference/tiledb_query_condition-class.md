# An S4 class for a TileDB QueryCondition object

An S4 class for a TileDB QueryCondition object

## Slots

- `ptr`:

  An external pointer to the underlying implementation

- `init`:

  A logical variable tracking if the query condition object has been
  initialized
