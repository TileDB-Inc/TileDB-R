# Display the 'AsBuilt' JSON string

Display the 'AsBuilt' JSON string

## Usage

``` r
tiledb_config_as_built_show()
```

## Value

Nothing is returned but as a side-effect the 'AsBuilt' string is
displayed
