# Retrieve the cached status of the last finalized query

This function accesses the status of the last query without requiring
the query object.

## Usage

``` r
tiledb_get_query_status()
```

## Value

The status of the last query
