# Set a parameter on the 'tiledb_profile' object

Set a parameter on the 'tiledb_profile' object

## Usage

``` r
tiledb_profile_set_param(profile, param, value)
```

## Arguments

- profile:

  A TileDB profile object

- param:

  The key for the new parameter

- value:

  The value for the new parameter
