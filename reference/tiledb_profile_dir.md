# Get the directory of a 'tiledb_profile' object

Get the directory of a 'tiledb_profile' object

## Usage

``` r
tiledb_profile_dir(profile)
```

## Arguments

- profile:

  A TileDB profile object

## Value

The directory of the 'tiledb_profile' object
