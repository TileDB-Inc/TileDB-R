# Remove a saved 'tiledb_profile'

Remove a saved 'tiledb_profile'

## Usage

``` r
tiledb_profile_remove(name = NULL, dir = NULL)
```

## Arguments

- name:

  (optional) Name of the profile to remove

- dir:

  (optional) Directory where the profile to remove is saved
