# Save the 'tiledb_profile' object

This will save the 'tiledb_profile' with the name and directory set at
creation.

## Usage

``` r
tiledb_profile_save(profile)
```

## Arguments

- profile:

  The 'tiledb_profile' object to save
