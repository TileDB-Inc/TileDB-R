# Create a 'tiledb_profile' object

Create a 'tiledb_profile' object

## Usage

``` r
tiledb_profile(name = NULL, dir = NULL)
```

## Arguments

- name:

  (optional) Name for the profile

- dir:

  (optional) Directory to create the profile in

## Value

A new 'tiledb_profile' object
