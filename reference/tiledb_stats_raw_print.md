# Print internal TileDB statistics as JSON

This function is a convenience wrapper for `tiledb_stats_raw_dump`. It
required TileDB Embedded 2.0.3 or later.

## Usage

``` r
tiledb_stats_raw_print()
```
