# Gets internal TileDB statistics as JSON string

This function is a (now deprecated) convenience wrapper for
`tiledb_stats_raw_dump` and returns the result as a JSON string. It
required TileDB Embedded 2.0.3 or later.

## Usage

``` r
tiledb_stats_raw_get()
```
