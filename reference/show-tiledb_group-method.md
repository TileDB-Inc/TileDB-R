# Display the TileDB Group object to STDOUT

Display the TileDB Group object to STDOUT

## Usage

``` r
# S4 method for class 'tiledb_group'
show(object)
```

## Arguments

- object:

  `tiledb_group` object
