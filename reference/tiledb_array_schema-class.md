# An S4 class for the TileDB array schema

An S4 class for the TileDB array schema

## Slots

- `ptr`:

  An external pointer to the underlying implementation

- `arrptr`:

  An optional external pointer to the underlying array, or NULL if
  missing
